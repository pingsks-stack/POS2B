export default function (context) {
  //console.log(context.store.state.auth.user.ref_id_role.position)
  //   if (context.store.state.auth.user.ref_id_role.position !== 'cashier') {
  //     return context.redirect('/member')
  //   }
  //   else {
  //     return context.redirect('/login')
  //   }
  //   if (context.store.state.auth.user.ref_id_role.position === 'member') {
  //     return context.redirect('/member')
  //     // //   } else if (context.store.state.auth.user.ref_id_role.position === 'cashier') {
  //     // //     return context.redirect('/seller')
  //     // //   }
  //   }else{
  //   }
}
